public class Notes {
    // Extends = Solid line + Arrow
    // use keyword extends
    // e.g. public class FinalExam3 extends GradedActivity {
    // Abstract = Solid Line + Arrow + Italics
    // use keyword _______
    // e.g. ______________________________________________
    // Interface = Dashed Line + Arrow + Italics
    // use keyword implements
    // e.g. public class FinalExam3 implements Relatable {
    // Final Exam = 16 questions
    // no imports no throws

    public static void main(String[] args) {
//        String s1 = "ES1036a";
//        String s2 = "ES1036a";
//        System.out.println(s1.equals(s2)); // only works for String datatype
//        System.out.println(s1 == s2);

            int[] shape = new int[3];
            shape[0] = 1;
            shape[1] = 2;
            shape[2] = 3;

            System.out.println(shape[0] + "\n" + shape[1] + "\n" + shape[2]);

//        int[] x = { 1, 2, 3, 4 };
//        int[] y = x;
//        x = new int[2];
//
//
//        for (int i = 0; i < x.length; i++)
//            System.out.print(y[i] + " ");
    }
}
